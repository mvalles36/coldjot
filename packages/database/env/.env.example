# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mailjot_dev

# Logging Configuration
LOG_LEVEL=info          # Options: debug, info, warn, error
                       # debug - Includes SQL queries in logs
                       # info  - Standard logging
                       # warn  - Only warnings and errors
                       # error - Only errors

# Environment
NODE_ENV=development    # Options: development, production, test
APP_ENV=development    # Options: development, production, test

# Prisma Configuration
# Connection Pool Settings (Optional)
# PRISMA_CONNECTION_LIMIT=5      # Maximum number of connections
# PRISMA_CONNECT_TIMEOUT=60000   # Connection timeout in milliseconds

# Note: Replace the DATABASE_URL with your actual database connection string
# Make sure to never commit sensitive credentials to version control 